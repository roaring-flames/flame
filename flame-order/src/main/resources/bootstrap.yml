spring:
  application:
    name: flame-order
  cloud:
    nacos:
      discovery:
        server-addr: 121.37.231.106:8848
        username: nacos
        password: nacos
      config:
        server-addr: 121.37.231.106:8848
        username: nacos
        password: nacos
        namespace: d538ad68-ebb4-4374-80a7-68cc220b00a1
        group: dev
        file-extension: yaml
        extension-configs:
          - data-id: flame-order.yml
            group: dev
            refresh: true
          - data-id: order.yml
            group: dev
            refresh: true
    alibaba:
      seata:
#      事务群组，要和下方vgroup-mapping保持一致（可以每个应用独立取名，也可以使用相同的名字），
#      要与服务端nacos-config.txt中service.vgroup_mapping中存在,并且要保证多个群组情况下后缀名要保持一致-tx_group
        tx-service-group: my_test_tx_group

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.104.183.2:3301/flame_order?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: jls5221318
    druid:
      connection-properties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
      #最大连接池数量
      max-active: 200
      # 配置获取连接等待超时的时间
      max-wait: 5000
      initial-size: 10
      min-idle: 10
      # 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒
      time-between-eviction-runs-millis: 60000
      # 配置一个连接在池中最小生存的时间，单位是毫秒
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1 FROM DUAL
#      建议配置为true，不影响性能，并且保证安全性。申请连接的时候检测，如果空闲时间大于timeBetweenEvictionRunMills，执行validationQuery检测连接是否有效
      test-while-idle: true
#      申请连接时执行validationQuery检测连接是否有效，做了这个配置会降低性能
#      test-on-borrow: false
#      归还连接时执行它validationQuery检测连接是否有效，做了这个配置会降低性能
#      test-on-return: false
      #打开PSCache，并且指定每个连接上PSCache的大小
      #是否缓存preparedStatement，也就是PsCache。PSCache对支持游标的数据库性能提升巨大，比如说oracle。在mysql下建议关闭
      pool-prepared-statements: true
      #要启用PSCache，必须配置大于0，当大于0时，poolPreparedStatements自动触发修改为true。在Druid中，不会存在Oracle下PSCache占用内存过多的问题，可以把这个数值配置打一下，比如说100
      max-pool-prepared-statement-per-connection-size: 20
      filters: stat,wall,slf4j
      use-global-data-source-stat: true
      # 通过connectProperties属性来打开mergeSql功能；慢SQL记录
      connect-properties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
      # 配置druid监控服务器
      stat-view-servlet:
        enabled: true
        login-username: admin
        login-password: 123456
        reset-enable: false
        url-pattern: /druid/*
        # 添加IP白名单
        #allow:
        # 添加IP黑名单，当白名单和黑名单重复时，黑名单优先级更高
        #deny:
      web-stat-filter:
        # 添加过滤规则
        url-pattern: /*
        # 忽略过滤格式
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*"

mybatisplus:
  workerId: 1
  datacenterId: 1
swagger:
  title: 订单服务
  description: 主要提供管理订单能力
  #扫描包路径
  url: com.flame.order.controller

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.flame.order.module.pojo


##seata配置
#seata:
#  enabled: true
#  application-id: flame-order
#  tx-service-group: my_test_tx_group
#  #默认AT模式
#  data-source-proxy-mode: AT
#  #是否开启数据源自动代理,默认为true
#  enable-auto-data-source-proxy: true
#  #（1）事务群组（可以每个应用独立取名，也可以使用相同的名字），
#  #要与服务端nacos-config.txt中service.vgroupMapping.my_test_tx_group=default,并且要保证多个群组情况下后缀名要保持一致-tx_group
#  service:
#    vgroup-mapping:
#      my_test_tx_group: default
#    # 降级开关
#    enable-degrade: false
#    # 禁用全局事务（默认false）
#    disable-global-transaction: false
#    grouplist:
#      default: 127.0.0.1:8091
## （2）seata配置中心
#  config:
#    type: nacos
#    nacos:
#      #nacos命名空间ID
#      namespace: a81a371c-19c8-48f4-bcda-877ed6193dd2
#      #nacos服务的地址
#      serverAddr: 121.37.231.106:8848
#      #seata分组名称
#      group: SEATA_GROUP
#      #nacos服务登录名称
#      username: nacos
#      #nacos服务登录密码
#      password: nacos
#  # （3）seata的注册中心
#  registry:  #registry根据seata服务端的registry配置
#    type: nacos
#    nacos:
#      #配置自己的seata服务
#      application: seata-server
#      server-addr: 121.37.231.106:8848
#      #seata分组名称
#      group: SEATA_GROUP
#      #nacos命名空间ID
#      namespace: a81a371c-19c8-48f4-bcda-877ed6193dd2
#      username: nacos
#      password: nacos

# feign组件超时设置，用于查看seata数据库中的临时数据内容
#feign:
#  client:
#    config:
#      default:
#        connect-timeout: 30000
#        read-timeout: 30000








